services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: freedom
      POSTGRES_PASSWORD: freedom
      POSTGRES_DB: freedom_cc
    ports:
      - "5432:5432"
    volumes:
      - freedom_pg:/var/lib/postgresql/data

volumes:
  freedom_pg:
